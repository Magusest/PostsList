enum AppRoutes {
    main = '/',
    post = 'posts',
    unfounded = '*'
}

const fetchingStock: number = 100;

const buttonInit: number = 5;

export {
    AppRoutes,
    fetchingStock,
    buttonInit,
}